const currencyFormat = new Intl.NumberFormat('pt-br', {
  style: 'currency',
  currency: 'BRL',
})

// Does not works in the browser
module.exports = class CurrencyManager {
  static format(value) {
    return currencyFormat.format(value)
  }
}
